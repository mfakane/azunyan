name = "Prettier"
shortcut = "Alt+Shift+F"
visibility = "whenAvailable"

[launch]
command = "prettier"
args = ["--write", "${file}"]   # or "--write ${file}"
workingDirectory = "${documentDirname}"
output = "reloadFile"

[when]
extensions = [".js", ".jsx", ".ts", ".tsx", ".json", ".css", ".scss", ".html"]

[env]
NODE_ENV = "development"
